# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode (generated by `xcodegen generate` — never commit)
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
build/

# CocoaPods
Pods/

# Swift Package Manager
.swiftpm/
.build/

# Okta tenant config — values come from build-machine env vars only.
# Never commit committed-config variants of these.
Okta.plist
*.xcconfig
.env
.env.*
